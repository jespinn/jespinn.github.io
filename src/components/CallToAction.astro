---
import svgPinkStar from '../assets/pink-star.svg'
import Button from './Button.astro'

const repoLink = 'https://github.com/jespinn/jespinn.github.io'
---

<section class="px-3 animate-fade" id="contacto">
  <div class="max-w-3xl mx-auto bg-white text-center flex flex-col gap-4 border-3 shadow-[8px_8px_0px_rgba(0,0,0,0.3)] relative p-10 hover:shadow-[12px_12px_0px_rgba(0,0,0,0.4)] transition-all duration-300">
    
    <h1 class="text-3xl font-semibold">Repositorio y Evidencias</h1>

    <p class="text-lg max-w-lg mx-auto">
      En este repositorio se encuentran las actividades, consultas SQL,
      diagramas entidad–relación y ejercicios desarrollados en la asignatura
      <strong>Bases de Datos I</strong>.
    </p>

    <div class="flex justify-center">
      <Button text="Ver Repositorio" link={repoLink} target="_blank" />
    </div>

    <img src={svgPinkStar.src} alt="decoración" width="80" class="absolute -top-10 right-20 animate-spin-slow" />
  </div>
</section>

<style>
/* Fade suave al aparecer */
.animate-fade {
  animation: fadeIn 0.8s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Giro lento de la estrellita */
.animate-spin-slow {
  animation: spin 12s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
</style>